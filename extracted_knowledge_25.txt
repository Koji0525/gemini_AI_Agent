【ナレッジ登録開始】
タイトル: 外部APIのレート制限問題
カテゴリ: API統合/レート制限
重要度: 中

【問題】
何が起きた: 外部APIのレート制限に引っかかりサービスが利用できなくなる
環境: 外部API連携, 高頻度呼び出し

【原因分析】
根本原因: API呼び出し頻度の制御が不十分
直接原因: 
1. レート制限の把握不足
2. エクスポネンシャルバックオフの未実装
3. 一括処理での過剰な呼び出し

【解決策】
解決手法: レート制限の遵守と適切なリトライ戦略
実装例:
import time
from functools import wraps

def rate_limited(max_per_second):
    min_interval = 1.0 / max_per_second
    def decorator(func):
        last_time_called = 0.0
        @wraps(func)
        def rate_limited_function(*args, **kwargs):
            nonlocal last_time_called
            elapsed = time.time() - last_time_called
            left_to_wait = min_interval - elapsed
            if left_to_wait > 0:
                time.sleep(left_to_wait)
            ret = func(*args, **kwargs)
            last_time_called = time.time()
            return ret
        return rate_limited_function
    return decorator

# 使用例
@rate_limited(10)  # 1秒間に10回まで
def call_external_api():
    # API呼び出し
    pass

【学び】
1. 外部APIのレート制限を必ず確認する
2. エクスポネンシャルバックオフを実装する
3. バッチ処理時は特に注意する

【予防策】
- APIコールの監視とアラート設定
- サンドボックス環境でのテスト
- フォールバックメカニズムの準備

成功率: 85%
【ナレッジ登録終了】
