【ナレッジ登録開始】
タイトル: 環境変数管理のベストプラクティス
カテゴリ: 環境設定/セキュリティ
重要度: 中

【問題】
何が起きた: 環境変数の読み込み失敗やセキュリティ問題が発生
環境: 複数環境(開発/本番), 機密情報管理

【原因分析】
根本原因: 環境変数の管理方法が一貫していない
直接原因: 
1. .envファイルのバージョン管理
2. デフォルト値の設定不足
3. 機密情報の平文保存

【解決策】
解決手法: 階層的な環境変数管理とセキュアな取り扱い
実装例:
import os
from typing import Optional

def get_env_var(key: str, default: Optional[str] = None) -> str:
    value = os.getenv(key)
    if value is None:
        if default is None:
            raise ValueError(f"環境変数 {key} が設定されていません")
        return default
    return value

# 使用例
DATABASE_URL = get_env_var('DATABASE_URL')
API_KEY = get_env_var('API_KEY', 'default_key')

# .env.exampleの提供
# .envは.gitignoreに追加

【学び】
1. 環境変数には適切なデフォルト値を設定する
2. 機密情報は環境変数で管理する
3. .envファイルはバージョン管理しない

【予防策】
- 環境変数の検証を実装する
- 機密情報の漏洩防止対策
- 環境ごとの設定を分離する

成功率: 92%
【ナレッジ登録終了】
