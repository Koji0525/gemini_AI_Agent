excluded_dirs:
  - _WIP
  - _BACKUP
  - _ARCHIVE
  - __pycache__
  - .git

excluded_files:
  - "*.pyc"
  - "*.log"
  - "*.tmp"

quality_gates:
  compile_check: true      # 構文チェックのみ
  linter: false           # 今回はスキップ
  formatter: false
  manual_review: false

auto_fix: false
