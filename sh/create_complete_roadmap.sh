#!/bin/bash
# 完全ロードマップ作成

cd /workspaces/gemini_AI_Agent

NOW_JST=$(TZ=Asia/Tokyo date +%y%m%d_%H%M)

cat > "MD/${NOW_JST}_COMPLETE_ROADMAP.md" << 'DOC'
# 🗺️ 完全ロードマップ - ナレッジベース見える化まで

## 📍 現在地（2025-11-21）

### ✅ 完了済み
- **Phase 1-3**: 高品質タスク実行 + 自動統合 + Git自動コミット
- **Phase 4A**: 堅牢な24時間自律稼働システム
  - APIレート制限管理
  - 緊急停止メカニズム
  - リソースクリーンアップ
  - エラーハンドリング
  - リアルタイムダッシュボード
  - F1初回実行
- **ナレッジベース基盤**: SQLite + KnowledgeManager
- **Flask Webアプリ**: 実装完了（未稼働）

### 📊 現状
- ナレッジ件数: **521件**
- 最終更新: 2025-11-09
- 問題: タスク実行時のナレッジ登録が未確認

---

## 🎯 ゴール

**ナレッジベースの完全可視化と活用**

1. ✅ すべてのナレッジを見える化
2. ✅ 品質チェックができる
3. ✅ リアルタイムで更新される
4. ✅ 長期的に使える状態

---

## 🚀 ロードマップ
```
現在地
  ↓
【Phase 4B】ナレッジベース完全稼働（今日～明日）
  ↓
【Phase 4C】実戦運用・検証（2-3日）
  ↓
【Phase 5】高度な分析・活用（1週間）
  ↓
【Phase 6】完全自律運用（継続）
```

---

## 📋 Phase 4B: ナレッジベース完全稼働

### 🎯 目標
**Flask Webアプリを実際に稼働させ、すべてのナレッジを確認できる状態にする**

### 実装内容

#### 1. Flask Webアプリの起動確認 ⭐⭐⭐（最優先）
**実装時間: 10分**
```bash
# 起動テスト
bash sh/start_knowledge_webapp.sh

# 確認項目:
# ✅ サーバーが起動する
# ✅ ポート5000でアクセスできる
# ✅ 521件すべて表示される
# ✅ 検索・フィルターが動作する
```

**成功基準**:
- ✅ ブラウザで全ナレッジが見える
- ✅ 検索が動作する
- ✅ ページネーションが動作する

---

#### 2. ナレッジ登録の動作確認 ⭐⭐⭐（必須）
**実装時間: 30分**
```bash
# Phase 3を実行してナレッジ登録をテスト
bash sh/run_phase3_full_autonomous.sh 2

# 確認:
# 1. 実行前の件数
sqlite3 knowledge.db "SELECT COUNT(*) FROM knowledge_entries;"
# → 521件

# 2. Phase 3実行

# 3. 実行後の件数
sqlite3 knowledge.db "SELECT COUNT(*) FROM knowledge_entries;"
# → 523件（+2件）に増えているはず

# 4. Flask Webアプリで確認
# ブラウザをリロード → 最新エントリが表示される
```

**成功基準**:
- ✅ Phase 3実行後にナレッジが増える
- ✅ Flask Webアプリに反映される
- ✅ 品質スコアが記録される

**問題があった場合**:
```bash
# ナレッジ登録のデバッグ
bash sh/debug_knowledge_registration.sh
```

---

#### 3. リアルタイム更新機能 ⭐⭐（推奨）
**実装時間: 1時間**

**現状**: ブラウザを手動でリロードが必要

**改善**: 自動リロード機能を追加
```html
<!-- templates/knowledge_list.html に追加 -->
<script>
// 30秒ごとに自動リロード
setInterval(() => {
    location.reload();
}, 30000);
</script>
```

または
```javascript
// Ajaxで部分更新（より高度）
setInterval(() => {
    fetch('/api/knowledge?limit=10')
        .then(response => response.json())
        .then(data => {
            // テーブルを更新
            updateTable(data.entries);
        });
}, 10000);
```

**成功基準**:
- ✅ 自動でデータが更新される
- ✅ ページ全体のリロードが不要

---

#### 4. 品質チェック機能の強化 ⭐⭐（推奨）
**実装時間: 2時間**

**追加機能**:

##### A. 品質フィルター
```python
@app.route('/knowledge')
def knowledge_list():
    # 品質スコアでフィルター
    quality_min = request.args.get('quality_min', 0)
    
    sql = '''
        SELECT * FROM knowledge_entries
        WHERE content LIKE '%quality_score: ' || ? || '%'
    '''
```

##### B. 品質アラート
```python
# 低品質エントリを検出
def check_quality_issues():
    """品質7.0未満のエントリを検出"""
    entries = get_entries_with_quality()
    
    low_quality = [e for e in entries if e['quality_score'] < 7.0]
    
    return {
        'count': len(low_quality),
        'entries': low_quality
    }
```

##### C. 品質トレンド
```python
# 時系列で品質の変化を表示
def get_quality_trend():
    """品質トレンドを取得"""
    # 日別の平均品質スコア
    # グラフ表示
```

**成功基準**:
- ✅ 品質でフィルターできる
- ✅ 低品質エントリを検出できる
- ✅ 品質トレンドが見える

---

#### 5. タグ管理システム ⭐（オプション）
**実装時間: 2時間**

**現状**: タグがメタデータに埋め込まれている

**改善**: タグで検索・フィルターできるようにする
```python
@app.route('/tags')
def tag_list():
    """タグ一覧"""
    # メタデータからタグを抽出
    tags = extract_all_tags()
    
    return render_template('tags.html', tags=tags)

@app.route('/knowledge')
def knowledge_list():
    # タグでフィルター
    tag = request.args.get('tag', '')
    
    if tag:
        sql += ' AND content LIKE ?'
        params.append(f'%{tag}%')
```

**成功基準**:
- ✅ タグ一覧が表示される
- ✅ タグでフィルターできる
- ✅ タグクラウド表示

---

### Phase 4Bの完了基準

#### 必須（⭐⭐⭐）
- ✅ Flask Webアプリが稼働
- ✅ 全ナレッジが見える
- ✅ ナレッジ登録が動作確認済み

#### 推奨（⭐⭐）
- ✅ リアルタイム更新
- ✅ 品質チェック機能

#### オプション（⭐）
- ✅ タグ管理

---

## 📋 Phase 4C: 実戦運用・検証

### 🎯 目標
**24時間自律稼働と連携して、ナレッジが自動的に貯まることを確認**

### 実装内容

#### 1. 24時間稼働テスト ⭐⭐⭐（必須）
**実施期間: 2-3日**
```bash
# 24時間稼働開始
bash sh/run_24h_robust_autonomous.sh

# 監視項目:
# 1. ナレッジ件数の増加
# 2. 品質スコアの分布
# 3. エラー発生状況
# 4. Flask Webアプリでの表示
```

**期待される結果**:
```
開始時: 521件
1時間後: 523件（+2件）
3時間後: 527件（+6件）
6時間後: 533件（+12件）
24時間後: 569件（+48件）
```

**確認ポイント**:
- ✅ タスク実行ごとにナレッジが増える
- ✅ Flask Webアプリに反映される
- ✅ 品質スコア10.0のエントリが多い
- ✅ エラーが少ない

---

#### 2. ナレッジ品質監視 ⭐⭐（推奨）
**実装時間: 1時間**

**毎日の品質レポート**:
```bash
# 日次レポート生成
bash sh/generate_daily_knowledge_report.sh

# レポート内容:
# - 今日追加されたナレッジ数
# - 平均品質スコア
# - 低品質エントリの有無
# - タグ分布
```

**成功基準**:
- ✅ 毎日自動でレポート生成
- ✅ 品質の低下を早期検知

---

#### 3. ナレッジ活用の検証 ⭐⭐（推奨）
**実施期間: 3日**

**検証項目**:

1. **検索機能の実用性**
   - よく検索するキーワードは？
   - 検索結果の精度は？

2. **フィルター機能の実用性**
   - 日付フィルターは使う？
   - 品質フィルターは使う？

3. **閲覧頻度**
   - どのくらいの頻度で見る？
   - どの情報が重要？

**成功基準**:
- ✅ 必要な情報がすぐ見つかる
- ✅ 品質チェックが簡単
- ✅ 長期的に使える

---

### Phase 4Cの完了基準

#### 必須（⭐⭐⭐）
- ✅ 24時間稼働でナレッジが自動蓄積
- ✅ Flask Webアプリで確認可能

#### 推奨（⭐⭐）
- ✅ 品質監視システム
- ✅ 実用性の検証

---

## 📋 Phase 5: 高度な分析・活用（1週間後～）

### 🎯 目標
**ナレッジを活用して開発効率をさらに向上**

### 実装内容

#### 1. ナレッジ活用システム ⭐⭐⭐
**実装時間: 1日**

**機能**:
- 類似ナレッジの推奨
- よく使われるパターンの抽出
- コード生成時にナレッジを参照
```python
def get_relevant_knowledge(task_id: str):
    """タスクに関連するナレッジを取得"""
    # タスクIDから関連ナレッジを検索
    # Gemini APIに渡してコード生成を改善
```

---

#### 2. 全文検索の強化 ⭐⭐
**実装時間: 2日**

**現状**: LIKE検索（簡易的）

**改善**: SQLite FTS5（全文検索）
```sql
-- FTS5テーブル作成
CREATE VIRTUAL TABLE knowledge_fts 
USING fts5(title, content);

-- 全文検索
SELECT * FROM knowledge_fts 
WHERE knowledge_fts MATCH 'Phase AND 4A';
```

**成功基準**:
- ✅ 高速な検索
- ✅ 複雑な検索クエリ対応

---

#### 3. 自動ドキュメント生成 ⭐
**実装時間: 2日**

**機能**:
- ナレッジから自動でドキュメント生成
- カテゴリ別にまとめる
- Markdown形式で出力
```python
def generate_documentation():
    """ナレッジから自動ドキュメント生成"""
    # カテゴリ別にグループ化
    # Markdownファイル生成
```

---

### Phase 5の完了基準

#### 推奨（⭐⭐）
- ✅ ナレッジ活用システム
- ✅ 全文検索強化

#### オプション（⭐）
- ✅ 自動ドキュメント生成

---

## 📋 Phase 6: 完全自律運用（継続）

### 🎯 目標
**人間の介入を最小限に、システムが自律的に成長**

### 実施内容

#### 1. 定期メンテナンス
- 週1回: ナレッジ品質チェック
- 月1回: システム健全性チェック
- 必要に応じて: 手動介入

#### 2. 継続的改善
- ナレッジの重複削除
- 低品質エントリの改善
- 新機能の追加

---

## 🎯 次にやること（優先順位順）

### 今日（2025-11-21）

#### 1. Flask Webアプリ起動確認 ⭐⭐⭐（10分）
```bash
bash sh/start_knowledge_webapp.sh
```
- [ ] サーバー起動確認
- [ ] ブラウザでアクセス
- [ ] 全ナレッジが見えることを確認

#### 2. ナレッジ登録の動作確認 ⭐⭐⭐（30分）
```bash
bash sh/run_phase3_full_autonomous.sh 2
```
- [ ] 実行前の件数記録
- [ ] Phase 3実行
- [ ] 実行後の件数確認
- [ ] Flask Webアプリで確認

#### 3. 問題があれば修正 ⭐⭐⭐（1時間）
- [ ] ナレッジ登録が動かない場合
- [ ] Flask Webアプリでエラーが出る場合
- [ ] デバッグ・修正

---

### 明日（2025-11-22）

#### 4. リアルタイム更新機能 ⭐⭐（1時間）
- [ ] 自動リロード実装
- [ ] 動作確認

#### 5. 品質チェック機能強化 ⭐⭐（2時間）
- [ ] 品質フィルター実装
- [ ] 品質アラート実装
- [ ] 動作確認

---

### 今週中（2025-11-23～24）

#### 6. 24時間稼働テスト ⭐⭐⭐（2-3日）
```bash
bash sh/run_24h_robust_autonomous.sh
```
- [ ] 稼働開始
- [ ] 定期的にチェック
- [ ] ナレッジ増加を確認

---

## 📊 成功基準（全体）

### Phase 4B完了時点
- ✅ Flask Webアプリが稼働
- ✅ 521件すべて見える
- ✅ ナレッジ登録が動作

### Phase 4C完了時点
- ✅ 24時間でナレッジが48件増加
- ✅ 品質10.0が80%以上
- ✅ エラー率3%以下

### Phase 5完了時点
- ✅ ナレッジ活用システム稼働
- ✅ 全文検索が高速
- ✅ 開発効率3倍達成

---

## 🗓️ タイムライン
```
2025-11-21（今日）
├─ Flask Webアプリ起動確認（10分）
├─ ナレッジ登録動作確認（30分）
└─ 問題修正（1時間）

2025-11-22（明日）
├─ リアルタイム更新（1時間）
└─ 品質チェック強化（2時間）

2025-11-23～24（今週）
└─ 24時間稼働テスト（2-3日）

2025-11-25～（来週～）
└─ Phase 5: 高度な分析・活用
```

---

## 📝 チェックリスト

### Phase 4B: ナレッジベース完全稼働

#### 必須
- [ ] Flask Webアプリ起動確認
- [ ] 全ナレッジ表示確認
- [ ] ナレッジ登録動作確認
- [ ] 検索機能動作確認
- [ ] フィルター機能動作確認

#### 推奨
- [ ] リアルタイム更新実装
- [ ] 品質フィルター実装
- [ ] 品質アラート実装

#### オプション
- [ ] タグ管理システム実装

### Phase 4C: 実戦運用・検証

#### 必須
- [ ] 24時間稼働テスト開始
- [ ] ナレッジ増加確認
- [ ] 品質確認

#### 推奨
- [ ] 日次レポート生成
- [ ] 実用性検証

---

## 🎯 最初の一歩

**今すぐやること**:
```bash
# 1. Flask Webアプリを起動
bash sh/start_knowledge_webapp.sh

# 2. ブラウザで開く
# VS Codeの「ポート」パネル → ポート5000 → 「ブラウザで開く」

# 3. 確認
# ✅ 521件すべて表示されるか？
# ✅ 検索は動くか？
# ✅ ページネーションは動くか？
```

**これが成功したら、次のステップへ進みましょう！**

DOC

echo "✅ 完全ロードマップ作成: MD/${NOW_JST}_COMPLETE_ROADMAP.md"

cat "MD/${NOW_JST}_COMPLETE_ROADMAP.md"

