# タスク 4: design

**概要**: WordPressにおける検索機能の具体的な実装仕様定義書作成（カスタムタクソノミー/カスタムフィールド検索の設計、検索対象項目、価格帯レンジ検索の実現方法）

**前提情報**: 依存タスクの出力を参照

---

ご依頼ありがとうございます。デザインの専門家として、承認されたCPTとカスタムフィールドの設計に基づき、M&Aマッチングプラットフォームにおける核となる機能である高度な検索機能の実装仕様定義書を作成します。この仕様は、ユーザーが効率的に目的の案件を見つけられるよう、カスタムフィールドとカスタムタクソノミーを組み合わせた柔軟な検索・フィルタリングを実現することを目的とします。タスクID: 4 M&Aプラットフォーム向け WordPress 高度検索機能 実装仕様定義書1. 検索対象CPTと基本方針項目仕様備考検索対象CPTma_case (案件情報)ユーザー（買い手）が案件を探すことが主目的。対象ユーザーログイン済みの買い手企業 (buyer_company)機密性を維持するため、検索結果の閲覧は認証済みユーザーに限定。基盤技術WP_Queryの拡張（pre_get_postsアクション）と、検索プラグインの利用（例: ACF Extended, SearchWP, ElasticPressなど）を組み合わせる。メタデータの検索効率を考慮し、大規模データの場合はインデックス型検索エンジンの導入を推奨。Export to Sheets2. 検索対象項目と実装方法案件情報 (ma_case) に対する主要な検索・フィルタリング項目とその実装仕様を定義します。2.1. テキスト検索（キーワード検索）項目検索対象フィールド実装方法キーワード1. 投稿タイトル (post_title) 
2. 投稿コンテンツ (post_content) 
3. 案件概要 (target_outline：カスタムフィールド)sパラメータを利用した標準検索に加え、カスタムフィールド(target_outline)はメタクエリ (meta_query)のLIKE検索で対象とする。Export to Sheets2.2. カスタムタクソノミー検索（分類フィルタリング）項目検索対象タクソノミー実装方法業種industryタクソノミークエリ (tax_query) を使用。複数選択 (OR/AND) に対応。地域regionタクソノミークエリ (tax_query) を使用。複数選択 (OR/AND) に対応。Export to Sheets2.3. カスタムフィールド検索（詳細フィルタリング）| 項目 | 検索対象フィールド（カスタムフィールド） | フィールドタイプ | 実装方法 |
| :--- | :--- | :--- |
| 案件ステータス | case_status | セレクト/ラジオボタン | メタクエリ (meta_query) で=（等しい）検索。 |
| 取引スキーム | deal_type | チェックボックス | メタクエリ (meta_query) でLIKEまたは配列検索。複数選択対応。 |3. 価格帯レンジ検索 (asking_price) の実現方法希望売却価格 (asking_price) は、ユーザーが設定した価格範囲でフィルタリングできることが必須です。3.1. フィールド定義とデータ型項目定義（再確認）データ型asking_price希望売却価格の目安数値/通貨 (ACFではnumberフィールド推奨)Export to Sheets3.2. 実装仕様（価格レンジ検索）ステップ実装内容詳細なWP_Queryでの処理1. ユーザー入力ユーザーが検索フォームで「最小価格 (Min)」と「最大価格 (Max)」を入力。フォームから2つのパラメータ (price_min, price_max)を取得。2. メタクエリの構築asking_priceに対して範囲検索 (BETWEEN) を行うメタクエリを作成。meta_query に以下を設定：
⎩⎨⎧​key: ’asking_price’value: [price_min, price_max]compare: ’BETWEEN’type: ’NUMERIC’​3. データ型の指定検索が正しく機能するため、必ず**type: 'NUMERIC'**を指定する。この指定により、WordPressが文字列としてではなく数値として価格を比較し、正確な範囲検索を実現します。Export to Sheets3.3. UX考慮事項価格レンジ検索UIは、フリー入力のテキストボックス、または一般的な価格帯を示すスライダーやプルダウンメニューを採用し、ユーザーが直感的に操作できるようにデザインします。4. 検索結果の並び替え（ソート）仕様ユーザーが検索結果を柔軟に分析できるように、以下のソートオプションを提供します。ソート項目データの種類実装方法備考新着順投稿日 (post_date)orderby: date, order: DESCデフォルトのソート順とする。価格順 (昇順/降順)カスタムフィールド (asking_price)orderby: meta_value_num, meta_key: asking_pricemeta_value_numを指定し、数値として正しくソートする。関連度順-検索プラグインの機能/orderby: relevanceキーワード検索時のみ適用可能。Export to Sheets5. 実装に関する推奨事項WP_Queryのフック利用: 検索フォームの処理は、functions.php 内の pre_get_posts アクションフックを利用して、メインクエリを変更することが最も効率的です。パフォーマンス: 複雑な**meta_queryやtax_queryを組み合わせるため、キャッシュプラグイン（例: W3 Total Cache, WP Rocket）や、可能であればサーバーサイドでのデータベース最適化**を初期段階から計画に含める必要があります。URLパラメータ: 検索条件は、再読み込みや共有のために、すべてURLパラメータ (?industry=it&price_min=1000&region=tokyoなど) として渡されるよう設計し、$_GET変数でクエリを構築します。