# タスク 4: 設計専門家

**概要**: WordPressにおける検索機能の具体的な実装仕様定義書作成（カスタムタクソノミー/カスタムフィールド検索の設計、検索対象項目、価格帯レンジ検索の実現方法）

**文字数**: 3042

---

承知いたしました。設計専門家として、WordPressの案件管理システム（CPT: ma_deal）における検索機能の実装仕様定義書を作成します。この検索機能は、標準的なタイトルやコンテンツ検索に加えて、カスタムタクソノミーとカスタムフィールドに格納された案件属性を対象とすることを主眼とします。WordPress 検索機能 実装仕様定義書1. 概要と基本方針本仕様は、カスタム投稿タイプ ma_deal (M&A案件情報) を対象とした、高度な検索機能の実装を定義します。検索は、カスタムフィールドの値とタクソノミー（カテゴリ）に基づき、ユーザーが求める案件を正確に絞り込めることを目標とします。項目詳細対象カスタム投稿タイプ (ma_deal) のみ。技術要素WordPress 標準の検索クエリ (WP_Query) とカスタムフィールド (meta_query)、タクソノミー (tax_query) を組み合わせたカスタム処理。フロントエンド検索フォームは専用のテンプレートで実装し、GETメソッドでクエリパラメータを送信する。Export to Sheets2. 検索対象項目の定義と設計検索フォームで利用する項目、およびそれに対応するデータソース（CPT、カスタムフィールド、タクソノミー）を定義します。No.検索項目名データソースフィールド名/スラッグ実装タイプ2.1キーワード検索CPT / ACFpost_title, post_content, seller_business_outlineテキスト入力2.2案件ステータスACFdeal_statusチェックボックス/セレクト2.3案件種別ACFdeal_typeラジオボタン/セレクト2.4売り手業種カスタムタクソノミーindustry (新規定義)チェックボックス/セレクト2.5所在地ACFseller_locationセレクト2.6希望価格帯ACFprice_range (価格データ化)レンジ検索 (後述)2.7直近売上高ACFseller_latest_salesレンジ検索 (後述)Export to Sheets2.1. カスタムタクソノミーの新規定義「売り手業種」を検索条件として利用するため、新たなカスタムタクソノミーを定義します。項目設定値説明タクソノミー名 (ラベル)業種管理画面で表示される名称。スラッグindustryクエリで使用するスラッグ。紐付けCPTma_deal階層あり (true)大分類（例：製造業）と中分類（例：自動車部品）を設定可能とする。Export to Sheets3. 検索クエリの実装仕様検索は、functions.php または専用クラス内で pre_get_posts アクションフック、または専用のテンプレートで WP_Query を直接使用して実行します。3.1. 標準的な検索クエリ (タクソノミー・ACF)カスタムクエリは、GETパラメータを取得し、以下の要素を組み合わせた複合検索とします。キーワード検索 (2.1):s パラメータで取得。標準のタイトル/コンテンツ検索に加えて、posts_where フィルターを使用して、カスタムフィールド (seller_business_outline など) の値も検索対象に含める。タクソノミー検索 (2.4):tax_query を使用。指定されたタームのスラッグを配列で受け取り、リレーションシップは AND (すべてに該当) または IN (いずれかに該当) で制御する。ACFによる条件検索 (2.2, 2.3, 2.5):meta_query を使用。複数の ACF 条件が指定された場合、リレーションシップは**AND**とする (例: ステータスが「初期検討」かつ所在が「東京」)。3.2. 価格帯・売上高レンジ検索 (2.6, 2.7) の実現方法ACFの「ナンバー」フィールドに格納された数値データ（売上高）および、価格帯を検索可能とするため、数値比較 (meta_query) を使用します。項目ACF フィールド名検索フォーム入力meta_query 実装仕様備考直近売上高seller_latest_salesmin_sales (最小値), max_sales (最大値)最小値が指定された場合: key = seller_latest_sales, value = min_sales, compare = >=, type = NUMERIC最大値も同様に compare = <= で実装。希望価格帯price_rangemin_price, max_price推奨：価格帯のデータは、数値型カスタムフィールドとして別途 price_min, price_max の2つに構造化して格納し、売上高と同様にレンジ検索を行う。price_range (テキスト型) に格納されている場合は、別途パース処理が必要となりパフォーマンスが低下するため非推奨。Export to Sheets3.3. クエリパラメータの例ユーザーが「業種：製造業」「売上高：1億円以上」で検索した場合の WP_Query の引数例：PHP$args = array(
    'post_type' => 'ma_deal',
    'tax_query' => array(
        array(
            'taxonomy' => 'industry',
            'field'    => 'slug',
            'terms'    => array('manufacturing'), // 製造業スラッグ
        ),
    ),
    'meta_query' => array(
        array( // 売上高 1億円以上
            'key'     => 'seller_latest_sales',
            'value'   => 10000, // 単位: 万円
            'type'    => 'NUMERIC',
            'compare' => '>=',
        ),
    ),
);
4. 考慮事項とパフォーマンス対策項目対策理由パフォーマンスmeta_query の多用はデータベース負荷が高い。検索頻度が高いカスタムフィールドに対しては、インデックスを適用することを検討する。 (例: seller_latest_sales )。価格データ構造ACFの価格帯フィールドは、表示用と検索用にデータを分離することを推奨。検索精度とパフォーマンス確保のため、数値比較が可能なデータ型で格納する。結果表示検索結果ページは、標準の search.php ではなく、カスタム投稿タイプ専用の archive-ma_deal.php テンプレートを使用し、検索フォームの状態を保持したまま表示する。ユーザーが検索条件を変更しやすいようにする。検索リセットフォーム内に「条件をクリア」ボタンを設置し、すべての検索クエリパラメータをリセット可能とする。UX（ユーザー体験）の向上。Export to Sheets